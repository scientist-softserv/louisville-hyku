#!/bin/sh

db-wait.sh "$DB_HOST:$DB_PORT"

PGPASSWORD=$DATABASE_PASSWORD psql -h $DATABASE_HOST -U $DATABASE_USER $POSTGRES_DB -c "CREATE DATABASE ${FCREPO_DB} WITH OWNER = ${DATABASE_USER}"
echo "${FCREPO_DB} created successfully!"
