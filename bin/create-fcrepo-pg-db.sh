#!/bin/sh

db-wait.sh "$DB_HOST:$DB_PORT"

PGPASSWORD=$DATABASE_PASSWORD psql -h $DATABASE_HOST -U $DATABASE_USER $POSTGRES_DB -c "CREATE DATABASE ${FCREPO_DB} OWNER = ${DATABASE_USER}"
#PGPASSWORD=$DATABASE_PASSWORD psql -h $DATABASE_HOST -U $DATABASE_USER $POSTGRES_DB -c "CREATE TABLE ${FCREPO_DB}.modeshape_repository()"
echo "${FCREPO_DB} created successfully!"
